# 网页剪藏

配合 Flota 浏览器扩展（`extensions/chrome-clipper`）使用：

1. 在 Flota「设置 → 网页剪藏」中生成 6 位配对码；
2. 在浏览器扩展弹窗中输入配对码完成配对；
3. 在任意网页点击扩展图标，选择「全文 / 选中内容 / 书签」剪藏。

剪藏内容保存到所选分类（默认「剪藏」），图片会下载到本地，同一链接不会重复保存。
可在设置中开启 AI 摘要与自动标签（需要已配置 AI）。
